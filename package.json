{
  "description": "",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "pnpm compile",
    "pretest": "pnpm compile",
    "posttest": "pnpm lint",
    "publish-cli-utils": "pnpm compile && pnpm publish build/packages/solana-cli-utils",
    "publish-test-utils": "pnpm compile && pnpm publish build/packages/solana-test-utils"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "gts": "^3.1.0",
    "typescript": "^4.0.3"
  },
  "pnpm": {
    "peerDependencyRules": {
      "ignoreMissing": [
        "react-native"
      ],
      "allowedVersions": {
        "jest": "28"
      }
    }
  }
}
